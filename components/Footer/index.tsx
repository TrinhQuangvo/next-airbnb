import React from 'react' 
import { footerLinks } from '@/constant';

type ColumnProps = {
    title: string;
    links: string[];
};

const Footer: React.FC = () => {

    return (
        <section className="">

        </section>
    )
}

export default Footer